{
  "name": "domain-look-up-servers",
  "version": "1.0.0",
  "description": "A crawler for IANA root database to generate domain lookup servers",
  "type": "module",
  "scripts": {
    "crawl": "bun run src/crawler.ts",
    "dev": "bun run --watch src/crawler.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
